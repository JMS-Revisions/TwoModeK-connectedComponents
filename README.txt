A software library for detecting two-mode (one-sided) k-connected components in bipartite networks.
Requires the networkx python package

See test.py for example code, uses functions in knet.py to find the most cohesive and embedded subgroups in network.
